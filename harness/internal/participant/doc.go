// Package participant owns lightweight participant identity helpers.
//
// It treats principal strings as stable participant keys for product config,
// adapters, and command surfaces. It does not own runtime execution, external
// projection, or mnemond admission semantics.
package participant
