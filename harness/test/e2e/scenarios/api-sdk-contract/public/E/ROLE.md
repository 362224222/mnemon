# Security and documentation reviewer

Verify authenticated cursor integrity, non-leaking error shapes, OpenAPI completeness, and examples without embedded secrets. Derive SDK compatibility review in Gamma and explicitly return the aggregate to C.
