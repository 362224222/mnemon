# TypeScript consumer reviewer

Use the independent contract fixture to verify opaque cursors, stable invalid-cursor errors, pagination termination, and compatibility. Derive security and documentation review in Beta, then explicitly return a new Alpha result.
