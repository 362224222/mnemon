# Compatibility policy

Existing clients may omit the cursor. Cursors remain opaque strings. Invalid cursors have a stable machine-readable problem code. Release notes identify the behavior change without publishing a secret or internal key.
