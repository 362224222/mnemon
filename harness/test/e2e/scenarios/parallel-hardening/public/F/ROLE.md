# Deployment compatibility reviewer

Verify the service works with read-only parent mounts plus a writable upload root, honors cancellation, avoids unsafe platform-specific rename assumptions, and passes the end-to-end upload fixture. Deliver only to E in Gamma.
