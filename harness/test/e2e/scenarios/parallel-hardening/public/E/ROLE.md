# Security and performance reviewer

Reject traversal, absolute paths, symlink escapes, partial-file publication, secret leakage, and object-sized buffering. Decline the first incomplete candidate once, then derive deployment compatibility to F and return a new Beta result.
